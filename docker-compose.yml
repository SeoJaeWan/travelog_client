version: "3.8"

services:
  travelog:
    image: sjw7324/travelog:latest
    container_name: travelog
    restart: unless-stopped
    volumes:
      - travelog_dist:/app/dist
    networks:
      - shared_network

networks:
  shared_network:
    external: true
